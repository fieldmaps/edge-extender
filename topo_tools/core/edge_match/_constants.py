"""Non-user-configurable constants for the match pipeline."""

# Tags the one orphan group made of zero-overlap children (gap-fill
# passthrough), guaranteed absent from real parent fids (always >= 0).
PASSTHROUGH_PARENT_FID = -1
